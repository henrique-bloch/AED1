#include <stdio.h>

#define MAX_PESSOAS 50005
#define MAX_ID 100005

int main() {
    int n, m, id;
    int fila[MAX_PESSOAS];
    int fora[MAX_ID] = {0}; // Vetor de marcação
    
    // Lê estado inicial
    if (scanf("%d", &n) != 1) return 0;
    for (int i = 0; i < n; i++) {
        scanf("%d", &fila[i]);
    }
    
    // Lê quem saiu e marca no vetor
    scanf("%d", &m);
    for (int i = 0; i < m; i++) {
        scanf("%d", &id);
        fora[id] = 1;
    }
    
    // Imprime quem permaneceu na ordem original
    int primeiro = 1;
    for (int i = 0; i < n; i++) {
        if (!fora[fila[i]]) {
            if (!primeiro) printf(" ");
            printf("%d", fila[i]);
            primeiro = 0;
        }
    }
    printf("\n");
    
    return 0;
}
